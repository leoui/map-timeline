{
  "name": "googlemap-visualizer",
  "version": "1.0.0",
  "description": "GoogleMap Visualizer — animate your Google Maps Timeline as an MP4 video, all processing stays on-device.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "mp4-muxer": "^5.1.3"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
